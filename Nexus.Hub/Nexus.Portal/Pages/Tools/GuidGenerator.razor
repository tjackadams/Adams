@page "/tools/guid"

<MudGrid Justify="Justify.SpaceEvenly">
    <MudItem xs="12">
        <MudStack Row="true" Justify="Justify.Center">
            <MudSpacer/>
            <MudTextField @bind-Value="@Value"
                          ReadOnly="true"
                          Adornment="Adornment.End"
                          AdornmentIcon="@Icons.Material.Filled.ContentCopy"
                          OnAdornmentClick="@(OnCopy)"/>
            <MudSpacer/>
        </MudStack>
    </MudItem>
    <MudItem xs="12">
        <MudStack Row="true" Justify="Justify.SpaceEvenly">
            <MudSwitch Value="@_settings.Uppercase" ValueChanged="FormatAsync" Label="@(_settings.Uppercase ? "Uppercase" : "Lowercase")" T="bool"/>
            <MudSwitch Value="@_settings.Brackets" ValueChanged="FormatAsync" Label="@(_settings.Brackets ? "Include Brackets" : "Exclude Brackets")" T="bool" />
            <MudSwitch Value="@_settings.Hyphens" ValueChanged="FormatAsync" Label="@(_settings.Hyphens ? "Include Hyphens" : "Exclude Hyphens")" T="bool" />
        </MudStack>
    </MudItem>
    <MudItem xs="12">
        <MudStack Row="true" Justify="Justify.Center">
            <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="@(GenerateAsync)">Generate</MudButton>
        </MudStack>
    </MudItem>
</MudGrid>