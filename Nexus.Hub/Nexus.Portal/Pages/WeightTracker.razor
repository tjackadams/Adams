@page "/tracker/weight"

@inherits BlazorStateComponent

<ClientStateProvider>
    <ClientSelection Clients="ClientState.Clients"></ClientSelection>
    <ClientMetricChart></ClientMetricChart>
    
    @if (ClientState.CurrentClient is not null)
    {
        <ClientMetricTable Client="ClientState.CurrentClient" Metrics="ClientState.CurrentClientMetrics"></ClientMetricTable>
    }

</ClientStateProvider>
