parameters:
  - name: projectName
    type: string

stages:
  - stage: ${{ replace(parameters.projectName, '.','_') }}_Build
    jobs:
      - template: ../jobs/docker_up_job.yml
        parameters:
          projectName: ${{ parameters.projectName }}
  