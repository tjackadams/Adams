name: adams

services:
- name: identity-api
  dockerFile: Services/Identity/Identity.Api/DockerFile
  dockerFileContext: .

  env:
  - ASPNETCORE_ENVIRONMENT=Development
  
  bindings:
  - port: 5105
    protocol: https